{
  "name": "agr-opentui",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun src/main.ts",
    "dev": "bun src/main.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "build": "make build",
    "check": "bun run typecheck && bun test"
  },
  "dependencies": {
    "@opentui/core": "^0.1.87"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11"
  }
}
