[project]
name = "agr-opentui"
version = "0.2.0"
description = "OpenTUI frontend for agr"
requires-python = ">=3.10"
dependencies = [
  "agr>=0.7.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
